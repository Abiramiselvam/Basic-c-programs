#include<stdio.h>
int main()
{
    int n,r,c=0;
    scanf("%d",&n);
    while(n)
    {
        r=n%2;
        if(r==1)
        {
            c=c+1;
        }
        n=n/2;
    }
    if(c>0)
    {
        printf("%d",c);
    }
    else
    {
        printf("Invalid");
    }
}
