#include<stdio.h>
int main()
{
    char a[30],len,b[50],i;
    scanf("%s",a);
    printf("A is %s",a);
    for(i=0;a[i]!='\0';i++)
    {
        b[i]=a[i];
    }
    b[i]='\0';
    printf("\nafter copying B is %s",b);
    
}
